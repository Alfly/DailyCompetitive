# DailyCompetitive

code: A.cpp, test: input.txt

```bash
../../run.sh A(.cpp) input(.txt)

# default is `input.txt`
../../run.sh A(.cpp)
```

test: stdin

```bash
../../run.sh A(.cpp) stdin
```

test: no input

```bash
../../run.sh A(.cpp)
```